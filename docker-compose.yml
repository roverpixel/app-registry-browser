version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - REGISTRY_URL=http://localhost:5000 # Example, can be overridden by the user
      - FLASK_ENV=development
